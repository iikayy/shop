# This config was automatically generated from your source code

version: 2.1
orbs:
  python: circleci/python@2
jobs:
  test:
    # Install dependencies and run tests
    docker:
      - image: cimg/base:2021.04
    working_directory: ~/pythonProject/shop
    steps:
      - checkout:
          path: ~/shop
      - run:
          name: Run tests
          command: |
            python -m venv venv
            . venv/bin/activate
            pip install -r requirements.txt

  deploy:
    executor: python-executor
    steps:
      - add_ssh_keys:
          fingerprints:
            - "SHA256:VMZGQ2s67SYpjuVTBWFkav0aL+Al8+2WyhfwgbAs6Yw"
      - checkout
      - run:
          name: Copy deployment script to EC2
          command: |
            scp -o StrictHostKeyChecking=no ec2-user@13.51.174.31:/home/pythonProject/shop
      - run:
          name: Run deployment script on EC2
          command: |
            ssh -o StrictHostKeyChecking=no ec2-user@13.51.174.31 'bash /home/pythonProject/shop/deploy.sh'

  workflows:
    version: 2
    test_and_deploy:
      jobs:
        - test
        - deploy:
            requires:
              - test